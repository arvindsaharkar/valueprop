import { ValuePropositionCanvas } from './value-proposition-canvas';

describe('ValuePropositionCanvas', () => {
  it('should create an instance', () => {
    expect(new ValuePropositionCanvas()).toBeTruthy();
  });
});
